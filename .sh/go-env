export GOROOT=/usr/local/go
export GOPATH=$HOME/work
for n in $GOROOT $GOPATH; do mkdir -p $n; done
...path-prepend $GOPATH/bin

source ~/.../src/chgo/share/chgo/chgo.sh
source ~/.../src/chgo/share/chgo/auto.sh
